{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Testing website for my future portfolio",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-dev": "sass -w --update --style=expanded css/sass:css",
    "sass-prod": "sass --no-source-map --style=compressed css/sass:css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.77.8"
  }
}
